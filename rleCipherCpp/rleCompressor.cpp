#include <fstream>
#include <iostream>
#include <string>
#include <vector>

int main() {
  std::ifstream inputFile("data.txt");

  if (inputFile.is_open()) {
    // for(int line=0;line < inputFile.)
  }
}
